# V31.10.20 Scanner3D Object-Mask

Questa patch elimina il finto 3D live e sostituisce la griglia globale con micro-celle solo sulla sagoma dell’oggetto.
Il GLB viene mostrato solo al build finale; prima del build la UI mostra solo stato geometrico reale.
